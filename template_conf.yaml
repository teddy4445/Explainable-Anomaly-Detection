---

# Batch parameters:
# -----------------

supervised_datasets:
#    - synt0.csv
#    - synt6.csv

unsupervised_datasets:
#    - synt0.csv
    - adult.csv

clf_models:
    RandomForest:
        - {depth: 5}

ad_models:
    IForest:
        - {contamination: 0.05}

# Experiment parameters:
# ----------------------

solvers:
#    BruteForce:
#        - {columns_num: 2, rows_num: 3}
#        - {}
#    Greedy:
#        - depth: 2
#    KNN:
#        - {k: 5}
#        - {k: 5, f_diff: []}
    MonteCarlo:
        - {time_limit_seconds: 600}
#    TeddyKNN:
#        - {k: 5}

scorers:
    linear:
        - {w_self_sim: 1.0, w_local_sim: 1.0, w_cluster_sim: 0, w_local_diff: 1.0, w_cluster_diff: 0, w_cov: 0, w_conc: 0.08}

similarity_metrics: 
    - inverse_euclidean
#    - mean_entropy
#    - cosine

alternative_explainers:
#    - acv
    - shap
    - lime
    - anchor

...